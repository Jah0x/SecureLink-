apiVersion: v1
kind: ConfigMap
metadata:
  name: securelink-bootstrap
data:
  bootstrap.sh: |
    #!/bin/sh
    # авто-миграции (postgres/sqlite), без интерактива
    npx -y drizzle-kit@latest push --config ./drizzle.config.ts || true

    exec npm run start
